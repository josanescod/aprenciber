# Definir els escenaris en format YAML

- dissenyar els camps dels escenaris: id, name, descript, description, difficulty, tags, containers
- llibreria per llegir fitxers yamls al backend `pip install pyyaml`
- crear el servei scenario_loader.py que llegeixi els escenaris
- valida i manejar errors dels fitxers yaml
- verificar que el backend pot llegir els escenaris