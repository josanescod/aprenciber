# Filtres panells admin

- Filtres del panell admin `AdminUsersView.vue` l'admin pot filtrar usuaris per rol o per actiu/inactiu.
- Solucionat problema, llista aules professors backend `classroom_repository`.
